version: '3.8'

services:
  weibo-crawler:
    build: .
    container_name: weibo-crawler-app
    ports:
      - "3001:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/posts.db
    restart: always
